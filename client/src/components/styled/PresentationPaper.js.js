import styled from 'styled-components';
import { Paper } from '@material-ui/core';

const PresentationPaper = styled(Paper)`
  && {
    background-color: #e5caae;
  }
`;

export default PresentationPaper;
