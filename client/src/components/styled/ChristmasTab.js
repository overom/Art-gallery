import styled from 'styled-components';
import { Tab } from '@material-ui/core';

const ChristmasTab = styled(Tab)`
  && {
    font-family: 'HelveticaNeue-Light', Helvetica;
    font-size: 1.4rem;
    text-transform: capitalize;
  }
`;

export default ChristmasTab;
