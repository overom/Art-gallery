import styled from 'styled-components';
import { CardContent } from '@material-ui/core';

const ChristmasCardContent = styled(CardContent)`
  && {
    padding-bottom: 0;
    padding-top: 10px;
    text-align: center;
  }
`;

export default ChristmasCardContent;
